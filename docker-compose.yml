---

version: "3.8"
services:
    cli:
        image: python:rc-buster
        container_name: "rob"
        environment:
            TZ: Europe/Berlin
        volumes:
            - ./src:/app/
            - ./resources/test-files:/tmp/rob-test-files
        tty: true
        networks:
            - default
        restart: 'no'
